# source: src/Vesting/Contracts/Vesting.hs
#
# Bug: pfindOwnInput inserts a stray '#' into 'pmatch (pfromData inp)'
#
# Apply with:
#   cp <repo>/src/Vesting/Contracts/Vesting.hs <case>/Plutarch.hs
#   (cd <case> && patch -p1 < Plutarch.patch)
#
--- a/Plutarch.hs
+++ b/Plutarch.hs
@@ -37,7 +37,7 @@
 pfindOwnInput = phoistAcyclic $ pfix #$ plam $ \self inputs ref ->
   pelimList
     ( \inp rest ->
-        pmatch (pfromData inp) $ \(PTxInInfo{ptxInInfo'outRef}) ->
+        pmatch (pfromData # inp) $ \(PTxInInfo{ptxInInfo'outRef}) ->
           pif (ptxInInfo'outRef #== ref) (pfromData inp) (self # rest # ref)
     )
     perror
