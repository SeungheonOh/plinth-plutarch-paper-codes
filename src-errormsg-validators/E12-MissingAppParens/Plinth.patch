# source: src/Vesting/Contracts/VestingPlinth.hs
#
# Bug: mkVestingValidator drops parens around (getRedeemer redeemer)
#
# Apply with:
#   cp <repo>/src/Vesting/Contracts/VestingPlinth.hs <case>/Plinth.hs
#   (cd <case> && patch -p1 < Plinth.patch)
#
--- a/Plinth.hs
+++ b/Plinth.hs
@@ -231,7 +231,7 @@
                 , vdDuration = duration
                 , vdAmount = amount
                 } = datum
-              red = unsafeFromBuiltinData @VestingRedeemer (getRedeemer redeemer)
+              red = unsafeFromBuiltinData @VestingRedeemer getRedeemer redeemer
            in vestingValidator
                 beneficiary
                 startTimestamp
