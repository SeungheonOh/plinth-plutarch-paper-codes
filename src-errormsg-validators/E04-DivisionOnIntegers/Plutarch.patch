# source: src/Vesting/Contracts/Vesting.hs
#
# Bug: plinearVesting uses Haskell '/' instead of 'pdiv'
#
# Apply with:
#   cp <repo>/src/Vesting/Contracts/Vesting.hs <case>/Plutarch.hs
#   (cd <case> && patch -p1 < Plutarch.patch)
#
--- a/Plutarch.hs
+++ b/Plutarch.hs
@@ -149,7 +149,7 @@
     ( pif
         (timestamp #> startTimestamp + duration)
         totalAllocation
-        (pdiv # (totalAllocation * (timestamp - startTimestamp)) # duration)
+        ((totalAllocation * (timestamp - startTimestamp)) / duration)
     )
 
 -- ============================================================================
